import { css } from '@emotion/react';
import Head from 'next/head';

const Discover = props => {
  return (
    <main id='discover'>
      <Head>
        <title>Canvas.IO - Discover</title>
      </Head>
    </main>
  );
};

export default Discover;
